[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --tb=short --strict-markers
markers =
    slow: tests that exercise subprocess (pdftotext) or network
    gpu: tests that require a CUDA device
